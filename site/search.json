{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"NiceShot!","text":"<p>An interactive dashboard for exploring tokamak plasma shot data.</p> <p>Point it at a shot-statistics file and it gives you:</p> <ul> <li>Projection view — UMAP or PCA scatter of every shot, coloured by any column</li> <li>Pairwise scatter — any two numeric columns plotted against each other</li> <li>Data table — sortable, searchable shot records</li> <li>Time traces — per-shot signal traces loaded on click (parquet, UDA, or SAL backends)</li> <li>SHAP decision plots — per-shot feature attribution (optional)</li> <li>Reference graph — reference-shot relationships overlaid on scatter plots (optional)</li> </ul> <p></p>","path":["NiceShot!"],"tags":[]},{"location":"#contents","level":2,"title":"Contents","text":"<ul> <li>Quickstart — install and run in five minutes</li> <li>Configuration — full reference for <code>config.yaml</code> and CLI flags</li> <li>Data Formats — what shot data, projection, and SHAP files must look like</li> </ul>","path":["NiceShot!"],"tags":[]},{"location":"#requirements","level":2,"title":"Requirements","text":"<ul> <li>Python ≥ 3.12</li> <li>uv for environment and dependency management</li> </ul>","path":["NiceShot!"],"tags":[]},{"location":"configuration/","level":1,"title":"Configuration","text":"<p>NiceShot reads a YAML config file at startup (<code>nice_shot/config.yaml</code> by default, overridable with <code>--config</code>). CLI flags take no precedence over config values — they control paths and server settings only.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#backend","level":2,"title":"<code>backend</code>","text":"<pre><code>backend: parquet   # parquet | uda | sal\n</code></pre> <p>Controls how per-shot time traces are loaded.</p> Value Behaviour <code>parquet</code> Reads <code>.parquet</code> or <code>.csv</code> files from <code>--data-dir</code>. The time-trace panel is hidden if the directory is absent or empty. <code>uda</code> Fetches live data from UDA via <code>uda-xarray</code>. URL form: <code>uda://&lt;signal&gt;:&lt;shot&gt;</code>. Requires <code>uda-xarray</code> installed separately. <code>sal</code> Fetches live data from SAL via <code>sal-xarray</code>. URL form: <code>sal://pulse/&lt;shot&gt;/&lt;signal&gt;</code>. Requires <code>sal-xarray</code> installed separately.","path":["Configuration"],"tags":[]},{"location":"configuration/#signals","level":2,"title":"<code>signals</code>","text":"<pre><code>signals:\n  - ip\n  - ne\n  - dalpha\n  - loopv\n  - plasma_energy\n</code></pre> <p>Signals shown in the time-trace panel. For the <code>parquet</code> backend these must match column names in the per-shot files. For <code>uda</code>/<code>sal</code> they are passed directly as signal names.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#time_window","level":2,"title":"<code>time_window</code>","text":"<pre><code>time_window:\n  min_time: 0.0\n  max_time: 1.0\n</code></pre> <p>Crop time traces to this window (seconds). Applied to all backends. <code>min_time</code> must be less than <code>max_time</code>.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#projection_method","level":2,"title":"<code>projection_method</code>","text":"<pre><code>projection_method: umap   # umap | pca\n</code></pre> <p>Algorithm used to reduce shot statistics to 2-D for the Projection tab.</p> Value Notes <code>umap</code> Non-linear; often preserves cluster structure better. Slower on first run; result is cached. <code>pca</code> Linear; fast and deterministic. No caching needed but cache is still written. <p>Changing this setting invalidates the projection cache and forces a recompute.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#umap_features","level":2,"title":"<code>umap_features</code>","text":"<pre><code>umap_features:\n  - ip_max\n  - ne_max\n  - ff_slope\n</code></pre> <p>Columns from the shot statistics file to use as features when computing the projection. Shots with <code>NaN</code> in any listed column are excluded. Defaults to all numeric columns (excluding <code>shot_id</code>) when omitted.</p> <p>Changing this list invalidates the cache.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#reference_shot_col","level":2,"title":"<code>reference_shot_col</code>","text":"<pre><code>reference_shot_col: reference__number\n</code></pre> <p>Column in the shot statistics file that holds the reference (parent) shot ID. When set, a toggle button appears in the left panel; enabling it draws the full connected reference graph on the scatter plots when a shot is clicked. Omit (or set to <code>null</code>) to hide the feature.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#uda-options","level":2,"title":"<code>uda</code> options","text":"<pre><code>uda:\n  timebase_hz: 1000\n</code></pre> <p>Only relevant when <code>backend: uda</code>. Interpolates all signals onto a uniform time grid at the given sample rate. If omitted, the native time axis of the first successfully loaded signal is used.</p>","path":["Configuration"],"tags":[]},{"location":"configuration/#example-mast-u-config","level":2,"title":"Example — MAST-U config","text":"<pre><code>backend: parquet\n\nsignals:\n  - ip\n  - ne\n  - tf_current\n  - plasma_energy\n  - loopv\n\ntime_window:\n  min_time: 0.0\n  max_time: 1.0\n\nprojection_method: umap\n\numap_features:\n  - ip_max\n  - ne_max\n  - bt_max\n  - betmhd_max\n  - wmhd_ipmax\n\nreference_shot_col: reference__number\n</code></pre>","path":["Configuration"],"tags":[]},{"location":"data-formats/","level":1,"title":"Data Formats","text":"","path":["Data Formats"],"tags":[]},{"location":"data-formats/#shot-statistics-file-shot-data","level":2,"title":"Shot statistics file (<code>--shot-data</code>)","text":"<p>A flat table of per-shot summary statistics. Accepted formats: <code>.parquet</code>, <code>.csv</code>.</p> <p>Required: one column that identifies the shot. The following names are detected automatically (in order of preference):</p> <pre><code>shot_id  shot  pulse  number  exp_number  pulse_id  shot_number\n</code></pre> <p>The column is renamed to <code>shot_id</code> internally regardless of its original name.</p> <p>All other columns can be anything. Object columns that can be coerced to numeric are converted automatically. Non-numeric columns are kept and available for coloring and filtering.</p> <p>Example schema:</p> shot_id ip_max ne_max breakdown_type reference__number 45000 1.2e6 3.5e19 ohmic 44990 45001 1.4e6 4.1e19 NBI 45000","path":["Data Formats"],"tags":[]},{"location":"data-formats/#per-shot-time-trace-files-data-dir-parquet-backend","level":2,"title":"Per-shot time trace files (<code>--data-dir</code>, parquet backend)","text":"<p>Each shot lives in its own file under <code>--data-dir</code>:</p> <pre><code>&lt;data-dir&gt;/\n  &lt;any-subdir&gt;/\n    &lt;shot_id&gt;.parquet   # or .csv\n</code></pre> <p>Required columns:</p> <ul> <li><code>time</code> — time in seconds (filtered to <code>time_window</code>)</li> <li>one column per signal listed in <code>signals</code> config</li> </ul> <p>The <code>&lt;any-subdir&gt;</code> layer is traversed but its name is not significant — all subdirectories are searched for a matching shot file.</p>","path":["Data Formats"],"tags":[]},{"location":"data-formats/#projection-files-projection","level":2,"title":"Projection files (<code>--projection</code>)","text":"<p>A pre-computed 2-D embedding that skips UMAP/PCA entirely. Three formats are accepted:</p>","path":["Data Formats"],"tags":[]},{"location":"data-formats/#numpy-npy","level":3,"title":"NumPy <code>.npy</code>","text":"<ul> <li>Shape <code>(n, 2)</code> — rows matched positionally to the shot data file (must have the same row count).</li> <li>Shape <code>(n, 3)</code> — first column is <code>shot_id</code>, next two are coordinates. Joined on <code>shot_id</code>.</li> </ul>","path":["Data Formats"],"tags":[]},{"location":"data-formats/#csv-or-parquet","level":3,"title":"CSV or Parquet","text":"<p>Must contain a shot ID column (same auto-detection as the shot stats file) and at least two coordinate columns. The first two non-shot-ID columns are used as X and Y axes; their names appear as axis labels in the UI.</p> <p>Example parquet schema:</p> shot_id umap_x umap_y 45000 2.31 -1.04 45001 2.44 -0.87","path":["Data Formats"],"tags":[]},{"location":"data-formats/#shap-values-file-shap-data","level":2,"title":"SHAP values file (<code>--shap-data</code>)","text":"<p>A NetCDF file (<code>.nc</code>) containing a single <code>xarray.DataArray</code> with two named dimensions:</p> <ul> <li><code>shot_id</code> — integer shot identifiers</li> <li><code>feature</code> — feature names (strings)</li> <li>one additional dimension for class (the index <code>True</code> / class=1 is used)</li> </ul> <p>The array is opened with <code>xr.open_dataset</code> and accessed via the default variable key <code>__xarray_dataarray_variable__</code>.</p> <p>The shot-to-index mapping is built from the shot statistics file at load time, so <code>shot_id</code> values in the SHAP file must be a subset of those in <code>--shot-data</code>.</p>","path":["Data Formats"],"tags":[]},{"location":"quickstart/","level":1,"title":"Quickstart","text":"","path":["Quickstart"],"tags":[]},{"location":"quickstart/#install","level":2,"title":"Install","text":"<pre><code>git clone &lt;repo&gt;\ncd nice_shot\nuv sync\n</code></pre> <p>Optional extras (SHAP plots, UDA/SAL backends):</p> <pre><code>uv sync --extra shap\n</code></pre>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#run","level":2,"title":"Run","text":"<pre><code>uv run python nice_shot/app.py --shot-data path/to/shot_stats.parquet\n</code></pre> <p>Open <code>http://localhost:8050</code> in a browser.</p> <p>The first run computes the UMAP projection and caches it. Subsequent starts load from cache unless the data file or <code>umap_features</code> list changes.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#minimal-example","level":2,"title":"Minimal example","text":"<p>If you have a single parquet file of shot statistics and nothing else:</p> <pre><code>uv run python nice_shot/app.py \\\n  --shot-data outputs/shot_stats.parquet \\\n  --no-debug\n</code></pre> <p>The time-trace panel is hidden automatically when <code>--data-dir</code> does not exist or is empty.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#with-per-shot-time-traces-parquet-backend","level":2,"title":"With per-shot time traces (parquet backend)","text":"<p>Lay out per-shot files under a directory:</p> <pre><code>data/mastu/\n  &lt;subdir&gt;/\n    45000.parquet\n    45001.parquet\n    ...\n</code></pre> <p>Each file must have a <code>time</code> column and one column per signal. Then:</p> <pre><code>uv run python nice_shot/app.py \\\n  --shot-data outputs/shot_stats.parquet \\\n  --data-dir data/mastu \\\n  --config configs/config_mastu.yml\n</code></pre>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#with-a-pre-computed-projection","level":2,"title":"With a pre-computed projection","text":"<p>Skip UMAP computation entirely by supplying your own 2-D embedding:</p> <pre><code>uv run python nice_shot/app.py \\\n  --shot-data outputs/shot_stats.parquet \\\n  --projection outputs/my_embedding.parquet\n</code></pre> <p>See Data Formats for accepted shapes.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#with-shap-values","level":2,"title":"With SHAP values","text":"<pre><code>uv run python nice_shot/app.py \\\n  --shot-data outputs/shot_stats.parquet \\\n  --shap-data outputs/shap_values.nc\n</code></pre> <p>A SHAP tab appears in the left panel. Click any point to see its decision plot.</p>","path":["Quickstart"],"tags":[]},{"location":"quickstart/#cli-reference","level":2,"title":"CLI reference","text":"Flag Default Description <code>--config PATH</code> <code>nice_shot/config.yaml</code> Path to YAML config file <code>--shot-data PATH</code> <code>outputs/shot_stats.parquet</code> Shot statistics file (<code>.csv</code> or <code>.parquet</code>) <code>--data-dir PATH</code> <code>data/mastu/</code> Directory of per-shot files for the parquet backend <code>--projection PATH</code> (none) Pre-computed 2-D embedding (skips UMAP/PCA) <code>--shap-data PATH</code> (none) SHAP values NetCDF file <code>--umap-cache PATH</code> platform cache dir Where to read/write the projection cache <code>--host HOST</code> <code>0.0.0.0</code> Bind address <code>--port PORT</code> <code>8050</code> Port <code>--debug / --no-debug</code> debug on Dash debug / hot-reload mode","path":["Quickstart"],"tags":[]}]}